{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Products",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}"
        }
    ]
}